#include "main.h"
/**
 * _isupper - function that checks for uppercase charachter.
 * @C: input
 * Return: 1 if c is uppercase, 0 otherwise
 */

int _is upper(int c)
{
if ((c >= 'A') && (c <= 'Z'))
return (1);
else
return (0);
}
